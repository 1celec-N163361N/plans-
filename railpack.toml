[phases.build]
cmds = [
  "npm install",
  "npx tsc -b lib/db lib/api-zod lib/api-client-react",
  "npm run build --workspace=@workspace/api-server",
  "BASE_PATH=/ npm run build --workspace=@workspace/khtat-sairak"
]

[start]
cmd = "npm run start --workspace=@workspace/api-server"